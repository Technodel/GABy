node_modules/
dist/
data/
*.env
.env
*.db
*.db-shm
*.db-wal
logs/
*.log
.DS_Store
Thumbs.db
src/renderer/dist/
bridge/dist/
bridge/node_modules/

# Temp and scratch files
_*.sqlite
_fix_db.*
_suny_test.*
_suny_setup.*
_.vps_*
temp_*
tmp_*
tmp-*
_temp_*
_*.cjs
_*.mjs
_*.ps1
_vps_*
_ws_*
_count_*
_dbcheck*
_dump_*
server_out.txt
server_err.txt
*.pyc
__pycache__/
*.tgz
*.bat

# ── Root-level VPS / utility / debug scripts (keep root clean) ────────────
/vps_*
/check_*
/_index_*
/run-*
/start-*

# Backup directories
SUNy-14B-Agent-Demo_bak/
comparison/

# Scripts directory — build.js is required for Docker build
scripts/*
!scripts/build.js

# VPS utility scripts — isolated to scripts/vps/, never track
/scripts/vps/*

# Prevent secret leaks
.aider.input.history
.aider-desk/
suny-vps.env
cleanup-*.*
vps-cleanup*

# Backup archives (too large for GitHub)
backups/
dist.zip

# ── Task execution test files (contain credentials) ──────────────────────────
/suny-task-test.js
suny-task-test-patched.js
suny-task-results*.json
suny-test-results*.json
suny-test-suite.js

# ── SSH/SCP debug logs (contain server IPs and auth info) ────────────────────
ssh_*.txt
scp_*.txt

# ── Root-level debug/test scripts (covered by /vps_*, /check_*, /run-*, /start-*) ──
/test_curl.sh
/test_login.cjs
/fix_db.py
/test_suny.py
/test_ws.py

# ── Temporary task-exec scaffolding ────────────────────────────────────────
task-exec-temp/
tmp_deploy/

# ── Bridge binaries (too large for git, generated during builds) ───────────
public/bridge/*.tgz
public/bridge/*.exe
public/bridge/*.zip
